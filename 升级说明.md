# V16 升级说明：桌面端信息密度 + 单图查看器

本补丁基于目前的 Cinematic 版本继续升级，不会覆盖 `assets/js/config.js`，不会修改 Supabase 数据、照片、摄影集名称、隐藏状态或管理员账号。

## 本次改动

### 1. DATA / 拍摄数据页
- 桌面端最大内容宽度限制为约 1380px，不再铺满超宽显示器。
- 标题、四个核心统计卡、环形图与参数图整体缩小。
- 大屏默认 3 列统计图，普通桌面 2 列，手机 1 列。
- 保留现有统计数据与计算逻辑。

### 2. ABOUT 页面
- 最大阅读宽度限制为约 1240px。
- 两栏内容更紧凑，正文不会横跨整个屏幕。
- 标题、宣言、个人资料层级都缩小到更适合电脑阅读的尺寸。

### 3. 新增单图查看器 `photo.html`
- 在 ARCHIVE 点击任意照片会进入单独大图页面。
- 摄影集详情页中的照片也可以点击进入。
- 图片保持完整比例，不裁切。
- 桌面端：左侧大图，右侧 EXIF。
- 手机端：大图在上，EXIF 在下。
- 支持 PREV / NEXT。
- 支持键盘 ← / → 浏览，Esc 返回摄影集。
- 隐藏摄影集中的照片不会通过查看器公开显示。

## 需要覆盖 / 新增的文件

将本补丁中的文件按相同路径上传到 GitHub：

```text
stats.html
about.html
archive.html
project.html
photo.html              ← 新增
assets/css/site.css
assets/js/archive-data.js
```

不要覆盖：

```text
assets/js/config.js
```

## 更新完成后

等待 GitHub Pages 发布，之后强制刷新：

- Windows：Ctrl + F5
- macOS：Command + Shift + R

## 单图查看逻辑

ARCHIVE 或摄影集详情 → 点击照片 → `photo.html` → 左右方向键浏览同一摄影集的前后照片。
