# V14 首页自动胶卷修复

本补丁修复 V13 首页横向作品集“需要滚轮或拖拽才移动”的问题。

## 修复内容

- 首页进入后约 0.45 秒自动开始向左缓慢流动。
- 自动速度由 18 px/s 调整为 30 px/s，更容易感知但仍保持缓慢电影感。
- 只有鼠标悬停在具体摄影集卡片上时才暂停；不再因为鼠标进入整块 Slider 区域就停止。
- 鼠标移出卡片后自动继续。
- 滚轮、拖拽操作期间临时暂停，约 1.2–1.6 秒后自动恢复。
- 修正无限循环距离计算，避免循环到末尾时因左侧 padding 产生跳动。
- 保留卡片点击进入摄影集、默认 Slider、时间排序和自适应封面选择。

## GitHub 只需覆盖

1. `index.html`
2. `assets/js/archive-data.js`

不要覆盖 `assets/js/config.js`。

更新后等待 GitHub Pages 发布，再按 `Ctrl + F5`（Mac：`Command + Shift + R`）强制刷新。
