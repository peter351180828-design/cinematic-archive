# V17 — 管理后台桌面优化 + 单图左右箭头

本补丁建立在 V16 单图查看器之上。

## 新增 / 调整

- 管理后台桌面端限制最大内容宽度，超宽屏不再把表单、统计和管理区域拉得过散。
- 管理标题、各功能区、Storage 仪表盘、上传区域都缩小到更适合桌面操作的密度。
- 照片管理在宽屏下最多 7 列缩略图；缩略图仍沿用“模糊底图 + 完整前景图”，不会为了方形卡片直接裁掉人像头部。
- 单图查看器在照片左右增加半透明 `PREV / NEXT` 边缘箭头。
- 箭头采用深色毛玻璃、暖金细边线，与当前 Cinematic / Gold UI 一致。
- 原有右侧 EXIF 面板、底部 PREV/NEXT、键盘方向键仍然保留。

## GitHub 需要覆盖

- `admin.html`
- `photo.html`
- `assets/css/site.css`
- `assets/js/archive-data.js`

不要覆盖 `assets/js/config.js`。

更新 GitHub Pages 后建议执行一次强制刷新：

- Windows：`Ctrl + F5`
- Mac：`Command + Shift + R`
