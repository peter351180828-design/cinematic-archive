# V15 自动胶卷引擎修复

这次只修首页横向摄影集自动流动，不修改 Supabase、后台、档案页或摄影集数据。

## 覆盖文件

- `index.html`
- `assets/js/archive-data.js`

不要覆盖 `assets/js/config.js`。

## 修复内容

1. 自动胶卷进入页面约 0.65 秒后直接启动。
2. 改用整数像素累积滚动，避免浏览器对小数 `scrollLeft` 取整后看起来完全不动。
3. 页面加载时鼠标即使刚好停在照片上，也不会阻止自动播放。
4. 只有实际移动鼠标进入某张作品卡后才暂停；移出卡片后继续。
5. 鼠标滚轮和拖拽仍可手动浏览，结束约 1.2–1.8 秒后自动恢复。
6. 保留无缝循环与摄影集点击进入功能。

覆盖后等待 GitHub Pages 发布，再按 Ctrl+F5（Mac：Command+Shift+R）强制刷新。
