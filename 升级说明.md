# V18 升级说明

本补丁解决三件事：

1. 单图查看器 EXIF 字号放大：标签约 10–10.5px，参数值约 17–18px。
2. 首页自动胶卷的作品卡恢复“单击进入摄影集”。旧版在 `pointerdown` 时立即把指针捕获给整条 Slider，部分浏览器会把点击重定向到 Slider 本身；V18 只有检测到真实拖拽后才启用 pointer capture。
3. 性能优化：
   - 自动胶卷只在作品区进入视口时运行；悬停、离开视口、切到 LIST、标签页隐藏时都会停止 RAF。
   - 自定义鼠标不再永久运行 RAF，鼠标停止后动画线程会休眠。
   - Reveal 动画共用一个 IntersectionObserver。
   - 胶片颗粒动画从 0.18s 放慢到 0.72s。
   - 档案/摄影集/管理缩略图使用 `content-visibility:auto`，大量照片时可跳过屏幕外卡片的绘制。
   - 动态图片增加 `decoding="async"`。

## 覆盖文件

- `index.html`
- `archive.html`
- `project.html`
- `photo.html`
- `stats.html`
- `about.html`
- `admin.html`
- `assets/css/site.css`
- `assets/js/site.js`
- `assets/js/archive-data.js`

不要覆盖 `assets/js/config.js`。
不需要修改 Supabase，不需要重新运行 SQL。

更新后等待 GitHub Pages 发布完成，然后 Windows 使用 `Ctrl + F5`，Mac 使用 `Command + Shift + R` 强制刷新。

## 关于“偶尔卡死”

目前照片数量已经接近 300 张。之前页面同时存在固定全屏 grain 动画、自定义鼠标永久 RAF、首页胶卷永久 RAF、多个独立 IntersectionObserver，以及大批 2560px JPEG 的解码/绘制。这些单项都不算严重，但叠加后在显存较小、集显笔记本、浏览器开很多标签页时会造成主线程或 GPU 短时间饱和，看起来像网页卡死。

V18 主要通过“离屏不工作、静止就休眠”降低持续占用。若以后照片增长到上千张，下一步应把档案页改成分页或虚拟列表，而不是一次建立全部照片 DOM。
