# V10 · Storage 使用量升级

这是针对已经在线运行的 Cinematic V9 网站的增量升级包。

## 需要覆盖的文件

- `admin.html`
- `assets/css/site.css`
- `assets/js/admin.js`

**不要覆盖 `assets/js/config.js`。** 本升级包本身也不包含它，因此不会影响现有 Supabase URL / Publishable key。

## 新增功能

管理后台新增 `STORAGE / 存储空间` 模块，会根据 `photos` 数据表中每张网页 JPEG 的 `bytes` 字段计算：

- 已使用空间
- 使用百分比
- 剩余空间
- 平均每张照片大小
- 按当前平均大小估算还能上传多少张照片
- 金色容量进度条；75% 以上警示，90% 以上变为危险提示

默认按 Supabase Free 的 **1 GB** 额度显示。

如果以后实际升级到 Pro，可以在后台把“仪表盘额度”改成 `PRO · 100 GB`。也支持自定义 GB 数值。

> 注意：这里的额度选择只影响你自己网站里的进度条显示，并不会购买或修改 Supabase 套餐。

## 为什么显示的是“估算”

本站统计的是数据库中有记录的网页照片文件大小。Supabase 的真实 Storage 用量还可能包含网站设置 JSON、孤立文件、同一 Organization 下其他项目等。因此实际计费与用量应以 Supabase Dashboard 的 Usage 页面为最终依据。

## 更新后

GitHub Commit 后等待 Pages 发布，再强制刷新：

- Windows：`Ctrl + F5`
- Mac：`Command + Shift + R`
