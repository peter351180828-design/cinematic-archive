# V13 首页自动胶卷升级

本补丁只调整首页作品集展示，不会修改 Supabase 配置、后台、照片、EXIF、摄影集名称/简介/显示隐藏状态。

## 新增

- 每次访问首页默认进入 `SLIDER` 横向作品栏；`LIST` 仍可临时切换。
- 当有 2 个或更多摄影集时，桌面端作品栏会像胶卷一样持续向左缓慢移动。
- 鼠标悬停在作品栏上会立即暂停；移开后继续移动。
- 鼠标拖动、滚轮横向浏览时会暂时停止自动移动，稍后恢复。
- 所有卡片（包括循环段）均可正常点击进入摄影集。
- 每个摄影集会自动从该摄影集照片中挑选更接近 `4:3` 横幅比例的封面，优先避免竖幅人像被大幅裁掉。
- 如果该摄影集没有合适横幅照片，则自动使用“完整照片 + 同图模糊背景”方式显示，不再硬裁头部。
- 手机端不自动滚动，保留手势横向浏览和 scroll-snap。

## 覆盖文件

将以下 3 个文件覆盖到 GitHub 仓库同一路径：

- `index.html`
- `assets/css/site.css`
- `assets/js/archive-data.js`

不要覆盖 `assets/js/config.js`。

GitHub Pages 发布完成后强制刷新：

- Windows：`Ctrl + F5`
- macOS：`Command + Shift + R`
