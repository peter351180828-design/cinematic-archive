# V8 摄影集展示开关升级

这次是在 V7「毛玻璃顶栏 + 多列档案」基础上增加摄影集级别的 VISIBLE / HIDDEN 控制。

## 新功能

- 管理后台新增「COLLECTION VISIBILITY / 摄影集展示」区块。
- 每个摄影集都有独立开关：VISIBLE / HIDDEN。
- 切换后立即保存到 Supabase Storage 中现有的 `site/site-settings.json`。
- HIDDEN 摄影集不会出现在：
  - 首页 Work / Selected
  - 首页 List 模式
  - Archive 档案页
  - Data 数据统计
  - Next Project 导航
  - 直接打开摄影集详情时会显示“目前设为 HIDDEN”而不展示照片
- 后台照片管理仍然可以看到隐藏摄影集，并可继续追加、编辑、批量删除。
- 隐藏不会删除照片、EXIF 或 Storage 文件。

## 更新方式

把升级包中的文件覆盖到 GitHub 仓库同名位置：

- `index.html`
- `archive.html`
- `project.html`
- `stats.html`
- `about.html`
- `admin.html`
- `assets/css/site.css`
- `assets/js/site.js`
- `assets/js/archive-data.js`
- `assets/js/admin.js`
- `assets/js/supabase-client.js`

不要覆盖 `assets/js/config.js`。

无需重新运行 SQL，也无需修改 Supabase Bucket 或 RLS。

## 使用

进入 `/admin.html` 登录后，在「摄影集展示」区块中直接切换摄影集状态。

- VISIBLE：公开网站正常展示。
- HIDDEN：从公开网页隐藏，但后台数据完整保留。

## 说明

这是“网站展示级隐藏”，用于控制哪些摄影集出现在你的网站中。由于当前 `photos` Bucket 本身是 Public，隐藏并不等同于加密或私有存储；已经知道某个图片直链的人仍可能访问该图片。如果以后需要真正的私密摄影集，可以再升级为 Private Bucket + Signed URL 权限体系。
