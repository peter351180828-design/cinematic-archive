# Cinematic Archive V9 升级说明

本补丁基于 V8，**不会覆盖 `assets/js/config.js`**，所以不会影响你现有的 Supabase URL、Publishable Key、照片、摄影集隐藏状态或管理员账号。

## 本次更新

1. **首页主图自动轮播**
   - 从当前公开摄影集中自动挑选更接近浏览器屏幕比例的照片。
   - 每约 8.5 秒柔和切换一次。
   - 比例差异较大的竖图/超宽图会完整显示，并使用同图模糊背景补足画面，不硬裁切。
   - 系统开启“减少动态效果”时停止自动轮播。

2. **顶部导航重新设计**
   - 从整条横向顶栏改成居中的悬浮胶囊。
   - 页面顶部只有轻微透明玻璃感，不再横贯整张照片。
   - 滚动后自动增强毛玻璃、阴影和金色细边。

3. **档案页多图浏览**
   - 大屏 5 列、常规桌面 4 列、中等屏幕 3 列、平板 2 列、手机 1 列。
   - 保留照片原始比例，不裁切。
   - COLLECTION 控制栏改成悬浮玻璃胶囊，不再形成贯穿全屏的黑色横带。

4. **摄影集详情页密度优化**
   - 大屏 4 列、桌面 3 列、中等屏幕 2 列、手机 1 列。
   - 不再一张照片占据大半个屏幕。
   - 图片仍保持原始比例，EXIF 位于图片下方。

5. **TIME / SHUFFLE**
   - 档案页和摄影集详情页都新增 `ORDER · TIME / SHUFFLE`。
   - TIME：保持原有时间排序。
   - SHUFFLE：随机排列；再次点击 SHUFFLE 会重新洗牌。
   - 浏览器会记住上一次选择。

## GitHub 需要覆盖的文件

把 ZIP 解压后，将以下文件上传到当前 Cinematic 网站仓库并选择覆盖：

- `index.html`
- `archive.html`
- `project.html`
- `about.html`
- `stats.html`
- `admin.html`
- `assets/css/site.css`
- `assets/js/archive-data.js`

**不要修改或覆盖 `assets/js/config.js`。**

提交后等待 GitHub Pages 发布，再强制刷新：

- Windows：`Ctrl + F5`
- Mac：`Command + Shift + R`
