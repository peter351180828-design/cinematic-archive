# V21 · 单图浏览逻辑与性能优化

本升级建立在 V20/V19 现有视觉系统之上，不修改 Supabase 配置、照片数据、摄影集简介、显示/隐藏状态或后台账号。

## 1. SHUFFLE 单图浏览改为“一次性随机牌组”

过去的问题：在 Archive 使用 SHUFFLE 后，点击一张随机位置的照片，单图查看器会重新按时间排序，因此可能一打开就是 `45 / 45`，右侧 NEXT 立即不可用。

V21 的规则：

- 在 Archive 或具体摄影集点击照片时，网站会保存你当时看到的实际排列顺序。
- TIME：单图查看器保留真实时间顺序与真实位置。
- SHUFFLE：你点击的那张照片自动成为本次浏览的 `01 / N · SHUFFLE`。
- 之后 NEXT 会把这一次随机牌组里其他照片各浏览一次。
- 到 `N / N` 后 NEXT 禁用，不会重新跳回第一张。
- PREV 只返回你在这一轮已经经过的照片。

因此随机浏览既不会“刚进入就是最后一张”，也不会无限循环。

## 2. 单图左右切换不再整页重载

V21 首次进入单图页时只读取一次照片数据。之后 PREV / NEXT：

- 不重新加载 HTML 页面
- 不重复请求整份 Supabase 照片列表
- 原地替换大图与 EXIF
- 同步更新浏览器地址栏和历史记录
- 只预加载前一张、后一张两张邻近照片

这会明显减少连续翻图时的卡顿、网络请求和页面重建。

## 3. Archive 分段加载进一步收紧

V21 将每批加载量再次降低：

- 大屏首批约 36 张
- 普通桌面首批约 28 张
- 手机首批约 18 张
- 后续每次约 14–20 张

同时 Archive 卡片取消逐张 Reveal Observer，图片继续使用 lazy loading + async decoding。

## 4. 页面纹理降低持续 GPU 压力

除 HOME 首页之外，颗粒/点阵纹理现在保持静态，不再持续刷新整张屏幕；首页的颗粒动画也进一步减速。背景质感仍然存在，但持续绘制成本更低。

## 覆盖文件

建议覆盖：

- `index.html`
- `archive.html`
- `project.html`
- `photo.html`
- `stats.html`
- `about.html`
- `admin.html`
- `assets/css/site.css`
- `assets/js/archive-data.js`

不要覆盖：

- `assets/js/config.js`

更新 GitHub Pages 后等待发布完成，再执行强制刷新：

- Windows：`Ctrl + F5`
- macOS：`Command + Shift + R`
