# V11 · 摄影集简介 / 去除单张照片说明

这次更新只调整摄影集详情页的文字逻辑，不需要重新运行 Supabase SQL。

## 新增

- 管理后台新增「摄影集简介」编辑区。
- 每个摄影集可以保存一段独立简介。
- 简介保存在现有 `site/site-settings.json` 中，不新增数据库表。

## 调整

- 摄影集详情页顶部不再显示“按时间排列 / 随机排列”等功能性说明文字。
- 顶部介绍改为你在后台保存的摄影集简介。
- 每张照片下面不再展示 `note` 文字说明，只保留 EXIF 信息。
- 上传时的“备注”改名为“私人备注（仅后台保存）”，旧备注数据不会删除，只是不再公开展示。

## 覆盖这些文件

```text
project.html
admin.html
assets/css/site.css
assets/js/archive-data.js
assets/js/admin.js
assets/js/supabase-client.js
```

`config.js` 不在升级包里，不会覆盖 Supabase 配置。
